# Ceiling pins added to prevent breaking changes on Docker rebuild.
# Review and update ceiling versions intentionally, not automatically.

# Production dependencies for AWS Lambda deployment
# Excludes test/dev dependencies to keep package size under 250MB limit

# Strands Agents SDK (GA)
strands-agents>=1.0.0,<2.0.0
strands-agents-tools>=0.2.20,<1.0.0

# AWS SDK
boto3>=1.35.0,<2.0.0

# PDF Generation
weasyprint>=62.0,<63.0
jinja2>=3.1.0
