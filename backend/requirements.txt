# Clew Directive Backend Dependencies
# Python 3.12+

# Agent framework
strands-agents>=1.0.0
strands-agents-tools>=0.2.20

# AWS SDK
boto3>=1.35.0

# PDF generation
# WeasyPrint 63+ required for compatibility with pydyf 0.11+
# pydyf 0.12+ removed the transform() method, breaking WeasyPrint 62.x
weasyprint>=63.0
jinja2>=3.1.0

# API server (local development)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
moto>=5.0.0  # AWS service mocking

# Linting (dev)
ruff>=0.8.0
mypy>=1.13.0
