# Clew Directive — Environment Configuration
# Copy to .env and fill in values.

# ──────────────────────────────────────
# ENVIRONMENT
# ──────────────────────────────────────
# Controls which runtime mode the backend runs in.
# "dev"     = local Docker with mocked Bedrock (default)
# "staging" = real AWS with lower limits
# "prod"    = full production deployment
CD_ENVIRONMENT=dev

# ──────────────────────────────────────
# AWS
# ──────────────────────────────────────
# Region for the Bedrock client and S3 bucket.
AWS_REGION=us-east-1
# AWS credentials (not needed for dev — uses mocked services)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ──────────────────────────────────────
# BEDROCK MODELS — NOTE
# ──────────────────────────────────────
# Model IDs are hardcoded in config/models.py (SCOUT_MODEL,
# NAVIGATOR_MODEL, CURATOR_MODEL). There are no environment
# variables for model selection — agents import their tier
# directly from that module.

# ──────────────────────────────────────
# S3
# ──────────────────────────────────────
# Bucket that holds directory.json and temporary briefing PDFs.
CD_S3_BUCKET=clew-directive-data
# S3 key where the curated resource directory is stored.
CD_DIRECTORY_KEY=data/directory.json

# ──────────────────────────────────────
# MONITORING (deploy time only)
# ──────────────────────────────────────
# Email address that receives CloudWatch alarm notifications via SNS.
# Required by the MonitoringStack at deploy time — it is never hardcoded in
# source. Supply it one of two ways when running `cdk deploy`:
#   - Export this env var:      ALARM_EMAIL=you@example.com
#   - Or pass it as CDK context: cdk deploy -c alarmEmail=you@example.com
# After the first deploy, confirm the subscription from the email AWS sends.
ALARM_EMAIL=

# ──────────────────────────────────────
# COST CONTROLS
# ──────────────────────────────────────
# Maximum number of briefing requests allowed per day.
CD_MAX_DAILY_REQUESTS=200
# Maximum total tokens allowed per session (across all LLM calls).
CD_MAX_TOKENS_SESSION=5000

# ──────────────────────────────────────
# FRONTEND
# ──────────────────────────────────────
# Base URL the Next.js frontend uses to call the backend API.
# Set to the API Gateway URL after CDK deployment for staging/prod.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
# Comma-separated list of allowed CORS origins for the backend.
CD_CORS_ORIGINS=http://localhost:3000

# ──────────────────────────────────────
# MONITORING / DEPLOYMENT
# ──────────────────────────────────────
# Email address that receives CloudWatch alarm notifications.
# Required by the CDK MonitoringStack — it is never hardcoded in source.
# Provide via this env var at deploy time, or pass CDK context:
#   cdk deploy -c alarmEmail=you@example.com
ALARM_EMAIL=

# ──────────────────────────────────────
# LOGGING
# ──────────────────────────────────────
# Log level for the backend. Defaults to DEBUG in dev, INFO otherwise.
CD_LOG_LEVEL=DEBUG
