/**
 * LearningPath Component — Displays recommended resources with live links.
 *
 * Each resource shows:
 *   - Name (clickable link to actual course)
 *   - Provider name
 *   - Why recommended (Navigator reasoning)
 *   - Difficulty/time/format badges
 *   - Free model indicator
 *   - Sequence note
 *
 * WCAG requirements:
 *   - Resource links have descriptive text (not "click here")
 *   - Badges use text + visual indicators (not color alone)
 *   - External links open in same tab (or announce new tab with aria-label)
 *   - List is semantically structured (<ol> for sequenced path)
 *
 * TODO: Implement with data from Navigator response
 */

export default function LearningPath() {
  // TODO: Implement
  return <div>LearningPath component — implement me</div>;
}
